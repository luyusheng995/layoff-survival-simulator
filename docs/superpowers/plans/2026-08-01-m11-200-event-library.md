# M11 200 Event Library Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Expand the random event library to the 200-event delivery requirement.

**Architecture:** Extend `src/data/events.js` with additional generated topic pools, keeping the `EVENTS` export shape unchanged. Verify through `tests/game.test.js` and the existing simulator.

**Tech Stack:** Browser JavaScript modules, Node test runner.

## Global Constraints

- Do not change the event schema.
- Do not change event selection APIs.
- Every choice must affect at least 2 values.
- Maintain at least 70/20/10 category counts: 140 daily, 40 crisis, 20 opportunity.

---

### Task 1: Event Count Tests

**Files:**
- Modify: `tests/game.test.js`

**Interfaces:**
- Consumes: `EVENTS`
- Consumes: `getEventStats`

- [ ] **Step 1: Add failing tests for M11 minimum counts and unique IDs.**
- [ ] **Step 2: Run `node --test` and confirm failure at 80 events.**

### Task 2: Event Library Expansion

**Files:**
- Modify: `src/data/events.js`

**Interfaces:**
- Produces: `EVENTS` with at least 200 items.

- [ ] **Step 1: Add generated daily, crisis, and opportunity topic pools.**
- [ ] **Step 2: Append them to `EVENTS`.**
- [ ] **Step 3: Run `node --test` and confirm pass.**
- [ ] **Step 4: Run `npm run simulate -- --runs 1000 --seed 20260731 --difficulty normal`.**
