# M5 Ending Funnel Plan

## Steps

1. Add red tests for soft final failures and `minDignity` tracking.
2. Implement `minDignity` in state metrics.
3. Add final-day ending funnel logic in `src/game/endings.js`.
4. Run unit tests and syntax checks.
5. Run normal-difficulty simulation and tune thresholds if `silent_survivor` still dominates or `quit_naked` is absent.
